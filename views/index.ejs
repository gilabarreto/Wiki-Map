<!DOCTYPE html>
<html lang="en">

<%- include('partials/_header') %>

<body>

  <% if(userId){ %>

      <h1>Your maps! 🥔</h1>
      <ul>
        <% for(let map of data) { %>
          <tr>
            <td> <a href="/maps/<%= map.id %>/map"><%= map.title %></a> </td>
            <td>
              <form id="fav-btn" action="/maps/<%= map.id %>" method="POST">
                <button type="submit">FAVOURITE</button>
              </form>
          </td>
          </tr>
        <% } %>
      </ul>

    <% } else{ %>

      <h1>Your maps! 🥔</h1>
      <ul>
        <% for(let map of data) { %>
          <tr>
            <td><a href="/maps/<%= map.id %>/map"><%= map.title %></a></td>
          </tr>
        <% } %>
      </ul>
      <% } %>
</body>

</html>
