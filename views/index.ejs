<!DOCTYPE html>
<html lang="en">

<%- include('partials/_header') %>

<body>

  <% if(userId){ %>

      <h1>Your maps! 🥔</h1>
      <ul>
        <% for(let map of data) { %>
          <tr>
            <td><%= map.title %></td>
            <td>
              <form id="fav-btn" action="/<%= map.id %>" method="POST">
                <button type="submit">FAVOURITE</button>
              </form>
          </td>
          </tr>
        <% } %>
      </ul>

    <% } else{ %>

      <h1>Your maps! 🥔</h1>
      <ul>
        <% for(let map of data) { %>
          <tr>
            <td><%= map.title %></td>
          </tr>
        <% } %>
      </ul>
      <% } %>
</body>

</html>
