<!DOCTYPE html>
<html lang="en">

<%- include('partials/_header') %>

<body>
  <% if(userId){ %>
    <div class="content">

    <h1>🥔 Your maps! 🥔</h1>
    <ul>
      <% for(let map of data) { %>
        <tr>
          <td> <a href="/maps/<%= map.id %>/map"><%= map.title %></a></td>
          <td>
            <form method="GET" action="/maps/<%= map.id %>/edit">
              <button class="standard-button" type="submit">EDIT</button>
            </form>
            <form method="POST" action="/maps/<%= map.id %>/delete">
              <button class="standard-button" type="submit">DELETE</button>
            </form>
          </td>
        </tr>
      <% } %>
    </ul>
  </div>

    <% } else{ %>
      <div class="content">

      <h1>🥔 You need to register to Create maps! 🥔</h1>
      </div>
      <% } %>

</body>

</html>
